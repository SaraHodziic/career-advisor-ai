# Improved UI snippets for your existing app

# 1. Add after st.set_page_config(...)
st.markdown("""
<style>
.block-container {
    padding-top: 2rem;
    padding-bottom: 2rem;
}

div[data-testid="metric-container"] {
    background-color: #f8f9fa;
    border: 1px solid #e5e7eb;
    border-radius: 10px;
    padding: 10px;
}

h1 {
    text-align: center;
}
</style>
""", unsafe_allow_html=True)

# 2. Replace title section with:

st.markdown("""
# AI Career Advisor

Analyze resumes, identify skill gaps, recommend jobs,
and compare resumes against job descriptions.
""")

# 3. Replace About Project evaluation section with metrics:

c1, c2, c3, c4 = st.columns(4)
c1.metric("Accuracy", "65.39%")
c2.metric("Precision", "68.56%")
c3.metric("Recall", "65.39%")
c4.metric("F1 Score", "64.97%")

# 4. Put Resume Strength and JD Match side by side:

left, right = st.columns(2)

with left:
    st.subheader("Resume Strength")
    st.progress(strength_score / 100)
    st.caption(f"Resume Strength Score: {strength_score}/100")

with right:
    if job_match_score is not None:
        st.subheader("Job Description Match")
        st.progress(job_match_score / 100)
        st.caption(f"Similarity Score: {job_match_score:.2f}%")
